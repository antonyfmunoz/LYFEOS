# LyfeOS production operations

## Release gate

Before a production release, run `npm run verify`, review `git diff --check`, and confirm the public `/api/ready` endpoint returns `{"status":"ready"}` after deployment. The included GitHub Actions workflow enforces the same build and test gate once it is pushed to the repository's `main` branch or used from a pull request.

## Availability and sessions

Fly checks `/api/ready`, which verifies a live database query. Authentication sessions are stored in Postgres table `session`, so a machine restart does not sign users out. The release migration `0009_postgres_sessions.sql` must be applied before enabling a release that uses the Postgres session store.

## Monitoring and alerts

GitHub Actions runs the `Production monitor` workflow every five minutes. It checks readiness, the expected anonymous `401` response from `/api/auth/me`, protected aggregate Health integrity, and the shape of the immutable release and migration identity. A failed check opens one labelled `production-monitor` incident issue with the failing workflow receipt. The first successful subsequent check comments on and closes that incident. Forced-failure run `32922225643` opened incident #13 and the next healthy run closed it; strengthened run `32945492151` passed against application revision `0acd45cab0ae09caf54e447e61a98dbfe0275f33`. Configure GitHub notification delivery to **Email** and **Only notify for failed workflows** for the `antonyfmunoz/LYFEOS` repository so incident creation and monitor failures reach the operational owner.

The same workflow can check `/api/operations/health-integrity` through a fail-closed server-only token. Generate one random value of at least 32 characters, store it as the Fly secret `LYFEOS_MONITOR_TOKEN`, and store the identical value as the GitHub Actions repository secret `LYFEOS_MONITOR_TOKEN`. Do not put it in client code, workflow output, issue text, or source control. Until both secret locations are configured and a scheduled run succeeds, the workflow emits a warning and health-integrity alerting is **not live**.

The manual `sentry_probe` workflow input sends one fixed, content-free error through `POST /api/operations/sentry-probe` using that same protected token. The route fails closed when Sentry is not configured, permits only one distributed attempt per hour, waits up to two seconds for SDK transport flush, and returns only normalized status plus the provider event ID. Run it only when qualifying Sentry or its routing rules. A successful workflow proves the deployed SDK accepted and flushed the event; confirm the matching event and notification in Sentry before calling provider receipt and alert routing live.

On 2026-08-26, the production Clerk instance was confirmed as a production environment using the verified `lyfeos.net` custom domain. Its Svix endpoint targets `https://lyfeos.net/api/webhooks/clerk`, subscribes exactly to `user.created`, `user.updated`, and `user.deleted`, and has its signing secret deployed server-side. A provider-sent example reported successful delivery, while an unsigned request returned `401`. Revision `2ed1694cb676c935f5db45756edb45f2909fbe7f` preserves locally owned password/profile state when a linked Clerk identity is deleted and performs full existing data-rights erasure only for provider-owned Clerk accounts. A marked synthetic account subsequently proved the real production create/update/delete lifecycle: creation produced one canonical user plus one stats, profile, integration and daily-log record; a provider name update synchronized; provider deletion erased the provider-owned account; both systems returned to their pre-test state. No verification message was requested or sent because the Backend API creates supplied identifiers as verified by default. The required isolated CI journey now always covers secure-cookie registration, username, login, session, logout and deletion. Real-browser/mobile onboarding and recovery remain separate acceptance evidence.

The integrity endpoint reports only aggregate counts and normalized status. It never returns user IDs, provider payloads, health values, notes, credentials, cursors, or vendor messages. It returns `503` for critical integrity defects, stale 15-minute sync/execution leases, overdue retry state, abandoned imports, repeated connector failures, or active connections carrying an error. Expected user-controlled deletion outcomes—such as a completed program session retaining a lost-link receipt, or a successful planning handoff whose mission was later deleted—remain informational and do not page the operator. A healthy report returns `200` with `status: "healthy"`; `critical`, `degraded`, and query-unavailable reports return `503` and use the existing incident workflow.

Each integrity snapshot runs inside a read-only transaction with a three-second database statement timeout. Concurrent endpoint requests share one in-flight snapshot instead of multiplying fleet scans. Before production activation, run `npm run ops:health-integrity` against an isolated current-schema staging branch. It performs three sequential read-only snapshots by default, prints only status, incident totals, and timing, and exits nonzero for a degraded/critical report, timeout, or query failure. `HEALTH_INTEGRITY_DRILL_ITERATIONS` may be set from `1` through `20`; keep the default for routine release qualification. This command is a staging query/failure receipt, not production load evidence and not a backup-restore drill.

On 2026-08-23, a disposable local PostgreSQL 16 logical-restore rehearsal used the complete `0000`-through-`0092` schema and non-empty representative account/Profile/Health rows. A custom-format backup was streamed into a new database; representative aggregate values and row checksums matched, all 119 tables, 1,477 columns, 277 indexes and 1,306 constraints matched, and the restored database returned a healthy zero-incident integrity report. Both source and restored databases were destroyed afterward. This qualifies only the local logical procedure; Neon point-in-time recovery, production-sized timing, access custody and managed-environment restoration still require a separately recorded drill.

A separate bounded local volume check on 2026-08-23 used 100 users and 160,000 representative Health rows: 100,000 hydration entries plus 20,000 workouts, 20,000 recovery activities and 20,000 observations. The three-run integrity drill remained healthy with a 47 ms maximum, and 20 concurrent token-authenticated monitor requests all returned `200/healthy` in 388 ms wall-clock. The disposable database was destroyed. This is an integrity-monitor capacity check only; it does not establish production traffic capacity or substitute for authenticated report/write/load testing with realistic production distributions.

On 2026-08-23, a separate authenticated local fixture loaded 12,500 owner-scoped factual hydration, recovery, workout, observation and nutrition records, 2,500 performed cardio-set records, and 2,500 planned program sessions. Each cold timeline, 10-year hydration/two-series trend, nutrition trend/CSV, workout history/CSV, cardio history and program report returned `200` within five seconds (observed maximum about 3.01 seconds; the optimized 2,500-session cardio response was about 192 ms). An 18-request mixed saturation batch completed within 18 seconds (observed about 13.2 seconds). Twenty-four simultaneous unique hydration mutations returned `201`; exact replays returned `200/replayed`, and changed-payload reuse returned `409`. Eighteen consecutive atomic-set corrections completed under five seconds and retained all 19 immutable workout revisions. A separate two-account rehearsal proved cross-owner reads/changes/deletes returned `404`, the owner export included representative Health records and excluded credential-reference fields, exact confirmation removed all substantive Health records, the minimal export/deletion receipts remained, and the account session still worked. These are disposable local integration receipts, not production capacity or device usability evidence.

On 2026-08-24, a fresh PostgreSQL 16 container applied all 100 migrations from `0000` through `0099`; the release runner then recorded all 91 release migrations from `0009` through `0099_native_message_interactions` and emitted no output on an exact second pass. A production-mode local server using production secure-cookie behavior behind an explicit trusted HTTPS proxy header completed six authenticated native-Messages journeys across three temporary accounts. The run proved anonymous denial, exact idempotent replay and changed-payload conflict, delivery/read receipts, private-note isolation, one-winner edit concurrency, bounded reaction toggles, cross-owner document denial, immutable SHA-256 attachment bytes after source mutation/deletion, revocation after message deletion, block/unblock delivery consent, and group last-admin/leave/remove/re-add behavior. All temporary accounts and the disposable database environment were removed after qualification. The `Verify` workflow now carries the same PostgreSQL 16 migration-convergence and six-journey Messages proof as a required credential-free job; that workflow definition has passed local parsing and contract tests but requires a pushed immutable revision before remote-CI status exists. This is clean-room migration and authenticated API evidence, not Clerk-provider, browser/device, managed-preview, production-migration, or deployed-source evidence.

On 2026-08-24, the transformation-intelligence increment advanced the clean schema to 101 raw migrations and 92 release entries through `0100_transformation_intelligence`; a fresh PostgreSQL 16 run exposed 137 public tables, and the release runner emitted no output on its exact second pass. A production build using secure cookies completed a six-test authenticated journey across separate owner/reviewer accounts: a Thread-bound canonical mission recorded planning context and explainable difficulty calibration; contract acceptance froze decision context and a versioned weighted rubric; the reviewer requested revisions against that rubric; the owner appealed; the same assigned reviewer reconsidered against the challenged review's immutable rubric snapshot and produced real capability XP; and reopening the supporting mission restored the prior capability total through a linked reversal rather than rewriting history. Both accounts were deleted after the run. The required PostgreSQL CI job now executes this journey beside the Messages journey; that workflow definition is locally tested, but has no remote status until the revision is pushed. This is local authenticated API and migration evidence only, not remote CI, Clerk-provider, browser/device, managed-preview, production-migration, or deployment evidence.

On 2026-08-24, the gamification-contract increment advanced the clean schema to 102 raw migrations and 93 release entries through `0101_gamification_contract`; a fresh PostgreSQL 16 container exposed 139 public tables and the release runner emitted no output on its exact second pass. `npm run verify` passed TypeScript, 98 test files with 424 passing tests and 48 explicitly gated tests, the production client/server/release build, and bundle budgets. A four-test production-mode authenticated journey used one temporary account to prove separate activity/consistency/capability/Health-practice/authority tracks, exact S-rank mission award and level transition, a mission-practice streak and supported badge, rejection of client-forged XP/level/streak/efficiency, linked XP and badge reversals on reopen, and exact re-earning without duplication. The account deletion left zero users and zero progression events. The required PostgreSQL CI job now includes this journey beside Messages and transformation intelligence. This remains local clean-room evidence—not remote CI, motivational/usability validation, Clerk-provider, managed-preview, production migration, immutable deployed source, or browser/device acceptance.

On 2026-08-24, the food-catalog readiness increment advanced the clean schema to 105 raw migrations and 96 release entries through `0104_food_catalog_gateway`; a fresh PostgreSQL 16 container exposed 148 public tables and the release runner performed an exact second-run no-op. A production-mode local server completed six catalog tests across two temporary accounts: anonymous denial, tampered-receipt rejection with zero writes, one explicit import plus idempotent replay, a distinct revised source snapshot, separate owner copies, manual-food correction without false catalog provenance, catalog correction that retained attribution and marked the source modified, and an attributed ingredient-label save whose server values came from the signed provider receipt rather than client text. Both accounts and the disposable database containers were removed. Contract tests also reject insecure non-local gateway URLs, partial configuration, undeclared item territories, malformed/extra provider fields, short secrets, expired or altered receipts and browser exposure of the gateway token. The required PostgreSQL CI job carries this journey, but no remote status exists until the revision is pushed. No licensed food provider or live gateway was selected, contacted or represented as active; real catalog, territory, attribution and known/unknown barcode evidence remain external gates.

The subsequent catalog-portions increment advanced the clean schema to 106 raw migrations and 97 release entries through `0105_food_catalog_portions`, still with 148 public tables and an exact second-run no-op. Seven production-mode API tests proved source-attributed portion import, idempotent replay with portions, distinct provider revisions, owner isolation, original catalog label/gram preservation after a private portion correction, and correct parent modification state; both temporary accounts and the exact disposable PostgreSQL container were removed. Focused contracts prove provider cursors are never returned raw, continuation receipts expire and reject alteration, pagination stays bound to the original query/provider/dataset, provider territory and portion-label invariants fail closed, and the UI appends deduplicated pages while displaying source portions. This is local activation-readiness evidence only: no provider, license, credential, production gateway, remote CI result, deployment, or real-device acceptance is implied.

The supported-browser label-reading increment adds no schema or migration. Its implementation keeps the selected image inside the browser, passes only an editable text draft into the existing ingredient-review form, requires the user's explicit save, and records `photo_ocr` rather than falsely claiming a manual or catalog source. Static contracts enforce the local detector wiring, disclosure and bounded deterministic text normalization; the production-mode API suite now includes an eighth journey proving that reviewed OCR text persists with its true capture method and without catalog provenance. This is implementation and local API evidence only. No image-upload or image-persistence path was added, but actual browser/device detector availability, recognition quality, accessibility and camera usability still require a recorded real-device acceptance matrix; manual entry remains the fail-closed fallback.

The spreadsheet structural-editing increments add no database migration. Existing version-one JSON documents parse with explicit 40-row/10-column defaults; new row and column insertions are bounded, preserve the original immutable client state, shift populated addresses plus affected formula/range references, and fail before mutation on structural or address overflow. Tab renames reject empty, oversized or case-insensitively duplicate names; confirmed removal cannot delete the last tab and deterministically activates a remaining neighbor without changing its cells. Range copy/paste uses bounded quoted tabular text, preserves raw formula input, supports multiline/tab/quote round trips, clears only explicitly pasted blank targets, and rejects malformed, oversized or out-of-grid clipboard input before replacing the client document. CSV/TSV import stays in the browser, is limited to 2 MB and the same sheet/cell bounds, discloses rows, columns, populated cells and formula inputs, resolves name collisions, and creates only a new unsaved tab after explicit confirmation. Optional bold, italic and alignment values are schema-governed, apply only to populated cells, persist through structural/input changes and clear independently of cell input; file and clipboard transfer intentionally omit presentation. This is deterministic local contract and build evidence only; authenticated persistence, concurrent editing, real file/clipboard permissions, mobile interaction and large-grid usability remain separate qualification gates.

The spreadsheet-history increment advanced the clean schema to 107 raw migrations and 98 release entries through `0106_spreadsheet_revisions`; a fresh PostgreSQL 16 container exposed 149 public tables and the release runner emitted no output on its exact second pass. A five-test production-mode authenticated journey across two temporary accounts proved anonymous denial, revision-one baseline creation, required write preconditions, exactly one winner from concurrent revision-one writes, stale-write conflict with the current revision, owner-private history, restore of version one as immutable version three, and rejection of a stale restore. Account deletion left zero users and zero spreadsheet revisions, and the exact disposable database container was removed. Static contracts also cover historical-snapshot validation, baseline migration, account export/erasure, bounded 20-state client Undo/Redo, and restore disclosure. This is local migration and authenticated API evidence; browser/mobile history interaction, real clipboard/file permissions, remote CI, managed preview, production migration and deployed-source evidence remain separate gates.

The governed-formatting increment adds no schema migration. Persisted cell documents now accept only three semantic numeric display formats and five named color tokens; arbitrary color strings and unsupported number formats fail validation. Decimal, percent and USD-currency presentation never rewrites raw input or formula results, CSV continues to export unformatted calculated values, and plain-text paste retains existing destination formatting without importing presentation. Focused tests cover deterministic positive/negative formatting, nonnumeric/error preservation, constrained schema validation, range application/reset, immutable source state and format-preserving paste. A local rendered-browser attempt was stopped before application initialization because Clerk's production frontend key correctly rejects the `127.0.0.1` origin and the synthetic qualification key has no reachable Clerk frontend; it is recorded as a browser coverage gap, not a pass. This is deterministic local contract/build evidence only; an approved Clerk development origin or deployed preview is required for theme contrast, assistive-technology behavior, real browser clipboard permissions and mobile toolbar acceptance.

The spreadsheet-windowing increment adds no schema migration. Maximum document capacity remains 500 rows by 100 columns, but the editor now derives a bounded visible row/column window with configurable clamped overscan and positions only that subset inside the full scroll canvas. At a 1,280×720 viewport, deterministic contracts cap both the top-left and bottom-right maximum-grid windows at no more than 400 mounted data cells rather than 50,000; invalid measurements, negative scroll and excessive overscan are normalized before index generation. Arrow keys move and refocus one bounded cell at a time, Shift+Arrow extends the existing range, off-screen targets scroll into view, and tab changes return the viewport to A1. This is structural scale evidence, not a browser timing result. Authenticated rendering, sticky-header behavior, focus continuity, assistive-technology semantics and scroll responsiveness still require the Clerk-compatible preview/device acceptance described above.

The Canvas-history increment advances the clean schema to 108 raw migrations by adding migration `0107_canvas_revisions.sql`. It adds a positive current revision to each Canvas and an owner-scoped immutable revision ledger with create, update and restore snapshots. Existing Canvases receive an idempotent baseline snapshot. Saves and restores require the current `x-lyfeos-expected-revision`, lock the owned Canvas row, reject stale writers, and append exactly one snapshot in the same transaction. Restore validates the historical version-one Canvas contract and writes it as a new version; it never rewrites history. Revision rows participate in account export and erasure. The editor separately retains up to 20 unsaved Canvas document states for local Undo/Redo, treats a full pointer drag as one history action, and resets that device-only history after save, reload, or restore. A JSON import no larger than 16 MB is parsed locally against the strict Canvas-v1 schema, summarized for explicit review, and only replaces the unsaved document after confirmation; it is still reversible and still requires Save. The isolated API suite exercises competing writes, owner isolation, restore and stale restore when a migrated test deployment is supplied; the default local suite skips that external database/browser gate.

The Canvas-interaction increment adds no schema migration. The existing version-one viewport is now authoritative in rendering: translations remain within ±10,000 pixels, zoom remains within 25–300%, zoom is anchored around the visible center, and fit-to-content derives one deterministic view from bounded node extents and the live viewport size. Background pointer movement pans the workspace; node movement divides screen deltas by the active zoom so persisted world coordinates remain accurate. Shift-selection, Select all and Clear drive grouped movement, color and deletion without changing the node/edge storage contract. Existing edge direction, bounded label and solid/dashed fields are now visible as arrowed labeled connections and editable from the single-node inspector. Each pan and grouped drag records one unsaved history snapshot rather than one per pointer event. These are deterministic local contract/build results; real mouse, touch, keyboard, focus and assistive-technology behavior still require authenticated browser/device acceptance.

The Canvas-template increment also adds no schema migration or provider dependency. Three module-load-validated built-in fixtures use the same strict Canvas-v1 document contract as persisted workspaces: Project map, Decision map and Reflection loop. Opening the library or selecting a template does not mutate the document. Apply discloses the current node and connection counts being replaced, creates one unsaved Undo snapshot, and still requires an explicit Save before persistence. The fixtures are product-owned starting structures, not a user-authored or remotely managed template catalog. Their contracts are locally testable; authenticated visual, keyboard, touch and assistive-technology acceptance remains an external release gate.

The Search-ranking increment advances the clean schema to 109 raw migrations with `0108_workspace_search_indexes.sql`. It enables PostgreSQL `pg_trgm`, adds six GIN full-text indexes and six GIN trigram title/name indexes across Missions, documents, Sheets, Canvas, Tables and relationships. Relationship indexing is intentionally limited to name, alias, company and job title; email, phone, notes and private relationship-intelligence records remain excluded. Runtime queries retain authenticated owner/deletion predicates and literal substring compatibility, then add full-text matching, typo-tolerant title/name matching and one cross-domain relevance sort capped by the caller's bounded limit. Ctrl/Cmd+K opens Search only outside active inputs, text areas, selects and content-editable surfaces. Local tests can qualify contracts and builds, but release qualification must confirm the extension and indexes migrate successfully, inspect the production query plan at realistic owner data volume, and exercise the authenticated desktop/mobile/screen-reader journey.

The first Table-operations increment adds no schema migration. Inline edits use the existing strict row PATCH contract and therefore revalidate the complete row against the current typed definition before replacement. Filter and sort derive an ephemeral visible list in the browser and do not create a second view or row authority. Bulk deletion accepts 1–100 unique positive row IDs and constrains the delete by the authenticated owner and owned database; the UI requires a count-specific irreversible-action confirmation. A stale or foreign row ID is never broadened into another owner's data. These are local contract/build results; authenticated database behavior, concurrent edits, realistic row-volume performance, mobile table interaction and assistive-technology behavior remain release gates.

The named Table-view increment advances the clean schema to 110 raw migrations with `0109_workspace_table_views.sql`. Each view belongs to one authenticated owner and one cascading Table, has a case-insensitively unique name within that Table, and stores only a strict version-one filter/sort/group definition. Create and update validate every referenced column against the current Table definition; definition edits are refused if they would invalidate an existing view. Applying or grouping a view derives presentation sections over the same row objects and never copies or mutates row authority. View deletion explicitly states that rows are retained. Views participate in account export and erasure. Release qualification must migrate a real database, exercise cross-owner and concurrent-name conflicts, and validate responsive and assistive-technology behavior.

The immutable Table-history increment advances the clean schema to 111 raw migrations with `0110_workspace_table_revisions.sql`. It adds positive current revisions to Tables and rows plus separate owner-scoped immutable ledgers; existing records receive idempotent version-one baselines. Table, row, form-submission, saved-view and form-definition writes serialize on the owned Table before validating the current definition. Table and row saves and restores require `x-lyfeos-expected-revision`, reject stale writers, and append the new snapshot in the same transaction. Restoring never rewrites history: it strictly parses the historical snapshot, validates it against current dependent records or the current Table definition, and creates a new version. Form submissions write both the canonical row and its initial revision atomically. Both ledgers participate in account export and erasure; deleting a row or Table intentionally cascades its history after the disclosed destructive confirmation. Local type, contract and build qualification does not prove the migration or competing-write behavior on the production database; release evidence must include migrated authenticated API concurrency/owner-isolation tests and desktop/mobile/assistive-technology acceptance.

The reviewed Table-CSV increment adds no schema migration or alternate row authority. Export covers every authoritative row, uses stable `column_id::name` headers, RFC-style quoting, raw typed values and leading-apostrophe protection for spreadsheet-executable prefixes. Import accepts a local `.csv` no larger than 2 MB and 500 source rows, maps each header exactly once by stable ID or one unambiguous current name, converts only the declared scalar types, validates every complete row against the current Table definition, skips disclosed blank rows, and presents row/column mapping counts before any request. Confirmation requires the reviewed Table revision, locks the Table, revalidates all rows, then creates all canonical rows and initial immutable revision snapshots in one transaction. The import is additive: it never overwrites or merges existing rows, and any invalid or stale request writes nothing. Local tests do not qualify browser file permissions, large-file responsiveness, spreadsheet interoperability, or production transaction behavior.

The Table relation/computation increment adds no schema migration. A relation stores only up to 50 unique positive row IDs from one authenticated-owner target Table; its label is derived from one stored scalar target column on read. Formula columns store a declared number, text, boolean, or date result and a bounded expression. The deterministic parser accepts literals, local stored-scalar/formula IDs, comparisons, arithmetic, and an explicit allow-list of conditional, empty-value, text, boolean, numeric, and UTC date functions; it has token, argument, reference, text-size, result-type, calendar-date, and cycle bounds and never evaluates dynamic code. Missing inputs remain blank where the operation permits it, while type, date, division, numeric, limit, and cycle failures remain visible bounded error values. Legacy numeric definitions default to number results. Rollups use one local relation and calculate count, or sum/average/min/max over one stored numeric target field. Formula, rollup and relation labels are projected into filtering, sorting, grouping and CSV export but are never copied into stored row values; Forms exclude formula/rollup fields. Every definition change validates the complete owner's Table graph, and every relation write/import/restore validates target-row ownership and existence. A transaction-scoped per-user advisory lock serializes Table graph mutations; referenced Tables and rows refuse deletion until forward links are removed, with self-references inside the same deletion set excluded. This increment does not create cross-table recursive formulas or cascading unlink behavior. Release qualification must exercise real PostgreSQL JSON-path reference guards, competing graph writes, large relation-option sets, browser controls, mobile interaction and assistive technology.

The governed Form increment advances the clean schema to 112 raw migrations with `0111_workspace_form_governance.sql`. Existing Forms are backfilled into a strict one-section version-one layout. Owners can organize all selected fields into bounded sections and add one forward-only visibility condition per optional target; server validation rechecks layouts and hidden-field submissions against the current Table definition. External respondent access is optional and scalar-only. Every grant has a purpose label, expiry, response cap, revocation state, random public ID and a 256-bit secret stored only as SHA-256. The one-time share URL carries the secret after `#`, so browsers do not send it in HTTP paths; the client moves it into the Authorization header. Public reads expose only selected field definitions and Form guidance. A locked transaction enforces expiry/cap/revocation, writes the canonical Table row and immutable first revision, appends a response receipt without answer copies or respondent identity, and increments the grant count. Account export excludes token hashes; erasure includes grants and receipts.

The reviewed backlink-batch increment adds no schema migration. The legacy unlink-all request remains compatible for exact, non-truncated sets of up to 500 references. The Table editor now exposes a source Table/relation filter and submits the exact reviewed source Table, source row, and relation-column identities for batches up to 500. Under the existing Table-domain advisory lock, the server rechecks ownership, target existence, each current relation definition, and the continued presence of the target ID before changing anything; a stale, duplicate, mismatched, forged, or oversized selection fails before mutation. Each affected source row is validated once, receives one update, and appends one immutable revision. A truncated result can therefore be cleared through explicit repeated batches without deleting either endpoint or creating a reciprocal relation store.

The reciprocal-relation increment adds no schema or duplicate backlink authority. Table reads derive incoming links from the authenticated owner's forward relation arrays and return source Table, row and relation labels. A reviewed unlink request is bounded to 500 references, requires an exact count-specific confirmation, takes the existing per-user Table-domain lock, re-derives the set, and refuses stale or truncated reviews. It removes only the chosen target ID from each source relation, revalidates the complete source row and remaining target ownership, increments each affected row version, and appends its immutable snapshot in the same transaction. Required relations fail closed if unlinking would empty them. Target and source rows are never deleted by this workflow. Real-PostgreSQL JSON-path behavior, large-account query performance, concurrency and browser accessibility remain release gates.

The Calendar keyboard-accessibility increment adds no schema migration and does not change Calendar's mission-backed authority. Previous/next/today controls and year/month/week/day selectors use labelled native buttons. Month and day selectors plus week/day time slots accept Enter or Space, expose contextual labels, and show visible keyboard focus. Mission chips are separate native edit buttons layered above the scheduling controls, so editing an existing mission cannot also open a create flow. These are structural source/build checks only; authenticated desktop/mobile keyboard, screen-reader, touch and scheduling-dialog acceptance remain release gates.

The Google Calendar reconciliation increment adds no schema migration. A cancelled event changes only its already-linked canonical mission to a visible `cancelled` status through the mission lifecycle; it does not delete the mission, completion evidence or user history. Calendar sync now follows Google's full/incremental synchronization contract instead of a silent single-page four-week read. The initial sync uses a fixed one-year lookback with no future cutoff, walks exact 250-event pages, and persists server-only continuation state after canonical mission reconciliation. Each request processes at most eight pages; an incomplete run returns explicit progress and resumes from the stored page token on the next user action. The final page replaces that continuation with a sync token; incremental pages reuse that token without forbidden time or order filters, include tombstones, and reset to a fresh full sync once after provider `410`. A session-level PostgreSQL advisory lock serializes one Calendar sync per user across instances. OAuth reconnect and disconnect clear provider cursor state, and account export strips it. Timed events retain their provider wall date/time instead of inheriting the app server timezone; malformed events and invalid all-day spans are skipped. Disconnect best-effort revokes the provider token, clears access/refresh credentials and expiry locally, retains imported Missions, and keeps the integration row in `revoked` state so a later explicit OAuth connection can reactivate it. Calendar and Tasks sync consume the shared API client's parsed response correctly and refetch canonical Missions before reporting completion. Unit protocol tests cover bounded full-sync continuation, incremental pagination, expired-token reset, state validation/export redaction and timezone-safe parsing. This is local contract/build evidence only; live Google token issuance, expiration, revocation, disconnect/reconnect, real multi-page responses and provider rate/error behavior remain external gates.

The general integration credential-vault increment advances the clean schema to 132 raw migrations with `0131_integration_provider_credential_vault.sql` and 123 release entries. Google access and refresh tokens are AES-256-GCM envelopes whose authenticated context binds owner, integration, provider and format version; only an opaque reference remains on the integration row. Startup migrates any legacy token rows before accepting traffic and fails closed if encryption cannot complete. Generic integration CRUD cannot inject credentials or create, mutate, or delete Google provider authority. Production configuration requires an explicit HTTPS redirect, client credentials and a 32-byte vault key. OAuth state is random, session/owner-bound, timing-safe, one-use and ten-minute bounded. LyfeOS intersects provider-reported grants with requested scopes, gates Calendar, Tasks and Drive independently, and never infers a denied permission. Task imports re-read the provider at mutation time rather than trusting browser-submitted task bodies. Refresh rotations stay encrypted. Disconnect attempts Google revocation, always destroys local custody, preserves imported LyfeOS records, and reports remote revocation as confirmed or unconfirmed. Provider errors are reduced to operation, internal owner and bounded status/type metadata rather than logging token-bearing response objects. This is source/local-gate evidence until an authorized Google account proves issuance, granular grants, refresh, reconciliation, revocation and reconnect behavior.

The Calendar range-loading increment advances the clean schema to 113 raw migrations with `0112_quests_calendar_window.sql`. A partial `(user_id, start_date, id)` index covers only scheduled, non-deleted canonical Missions. The protected range endpoint requires the session owner, valid ordered calendar dates, no more than 370 days, a page size of 1–250, and a valid in-range base64url cursor. It returns private/no-store pages ordered by date and ID; malformed, foreign, deleted, unscheduled and out-of-window records fail closed or remain excluded. The dedicated `/calendar` route skips the prior full-history context load, derives its visible day/week/month-grid/year window deterministically, and exposes loading, retry and explicit Load more states. Board/List still force the established complete mission load, and successful mission mutations invalidate Calendar pages. On 2026-08-25, a fresh disposable PostgreSQL 16 environment applied all 113 raw migrations, reconciled all 104 release entries, and produced an exact zero-output second pass. A three-test production-mode authenticated journey across two temporary accounts proved anonymous denial, cross-owner denial, stable two-page cursor order, exclusion of archived, unscheduled, out-of-window and foreign-owner Missions, private/no-store responses, and rejection of oversized/reversed ranges plus malformed/out-of-range cursors. Against a rolled-back 100,000-row fixture, PostgreSQL used `quests_user_calendar_window_idx` directly with no sort and completed the 251-row cursor query in approximately 0.38 ms. The server, accounts and exact disposable container were removed afterward. This is local clean-room migration/API/query-plan evidence, not remote CI, deployed migration, production traffic, authenticated browser interaction or assistive-technology evidence.

The Calendar offline-concurrency increment advances the clean schema to 114 raw migrations with `0113_calendar_offline_concurrency.sql` and 105 release entries. A database trigger advances a positive server-owned mission revision on every persisted update. Calendar creates and edits carry bounded mutation identities; creates also retain an internal lifecycle key, while metadata-only owner-scoped receipts retain a canonical payload hash, operation, mission ID and resulting version. Exact retries return the accepted mission, changed-payload identity reuse fails, and an update succeeds only against the expected mission revision. Conflict responses disclose only the current title, schedule and revision; internal lifecycle keys and hashes are removed from mission API responses. The browser keeps at most 100 unsynced creates/edits in private IndexedDB, syncs pending records in order on reconnect, coalesces repeated offline edits only before any network attempt, and retains rejected or conflicting changes for retry/discard. A conflict never auto-overwrites: the user must keep the server version or explicitly apply queued fields against the disclosed current revision. Completion and archive are not queued because they can change XP, capability review state, automations and cross-product work. Mutation receipts participate in account export and erasure. On 2026-08-25, a fresh disposable PostgreSQL 16 environment applied all 114 raw migrations, reconciled all 105 release entries and produced an exact zero-output second pass. A production-mode authenticated journey proved exact create replay, changed-payload rejection, revision-one create, revision-two edit, exact edit replay, stale conflict with current-version disclosure, explicit revision-three reapply, owner receipt export and account erasure; the existing two-owner Calendar-window journey also passed. Temporary accounts, server and the exact container were removed. This is local database/API/build evidence, not remote CI, deployed migration, service-worker cold-start offline support, or real browser/device reconnect and storage-eviction evidence.

The workflow-automation recovery increment advances the clean schema to 115 raw migrations with `0114_workflow_automation_recovery.sql` and 106 release entries. Every new run retains an immutable rule snapshot, while one owner-scoped receipt per action tracks the action index, bounded status, attempt count, target mission and—for category updates—the mission revision claimed before the first attempt. A concurrent duplicate manual request returns the existing run and cannot repeat its effects. Explicit repair skips succeeded actions, uses a lifecycle-keyed follow-up mission to converge after uncertain completion, and refuses to overwrite a category changed after the original claim. A rule is automatically paused after three consecutive failed runs; explicitly enabling it resets that circuit breaker. Run responses do not expose mutation identities or definition snapshots, and action receipts participate in account export and erasure. On 2026-08-25, a fresh disposable PostgreSQL 16 environment applied all 115 raw migrations, reconciled all 106 release entries, and produced an exact zero-output second release pass. A production-mode authenticated API journey proved concurrent duplicate convergence, a two-action run with one follow-up, failed-action-only repair without duplicate follow-up creation, cross-account 404 isolation, refusal of three ambiguous stale category recoveries, automatic pause at the third consecutive failure, preservation of the user's category, owner export, and account erasure. Required CI `33177640480` repeated that journey on 2026-08-28 against disposable PostgreSQL, including exact migration convergence, isolated backup/restore, owner cleanup, bounded public action-result fields and generic per-action failure codes. Release 111 renders each receipt as an ordered, accessible run record with start/completion time, trigger Mission ID, bounded schedule context, action type/status/attempts/target Mission ID, safe error codes, deterministic selectors and an explicitly labelled unfinished-action repair control. This is exact CI and deployed implementation evidence; failed/partial/running receipt rendering and Repair activation still need a disposable rendered browser fixture and human assistive-technology review.

The Project lifecycle-recovery increment advances the clean schema to 116 raw migrations with `0115_project_lifecycle_recovery.sql` and 107 release entries. Existing board rows without a native Project-created event are marked `legacy_kanban` and receive a migration provenance event; users explicitly review and reconcile them. Project/Mission membership now locks both aggregates, checks both revisions, changes the canonical Mission row and Project revision together, and records exactly one semantic event. This serializes Project completion against new open-Mission links and refuses silent movement from another Project. Project-created Missions use a stable mutation identity, internal lifecycle key and payload hash so exact retries converge while changed-payload reuse fails. Project removal is a recoverable soft removal available only after archive, exact-title confirmation and unlinking all Missions; restoration preserves the archived state and full history. On 2026-08-25, a fresh disposable PostgreSQL 16 environment applied all 116 raw migrations, reconciled all 107 release entries, and produced an exact zero-output second release pass. A production-mode authenticated six-test API journey proved concurrent link convergence with one event, cross-Project move refusal, exact create replay and changed-payload rejection, completion/link serialization, explicit legacy reconciliation, governed remove/restore, cross-account 404 isolation, account export, and exact erasure. Temporary accounts contained zero residual users, Projects, Project events, or Missions after cleanup. This is local database/API/build evidence, not remote CI, deployed migration, or real browser/device interaction evidence.

The data-rights contract is now `lyfeos.data-rights.v3`, a machine-readable description of current product behavior across thirteen data classes. V3 adds consent-bound team and coach collaboration as a purpose-bound participant class; membership itself grants no personal-record access, while each Mission or Thread projection has an explicit recipient, purpose, summary/status scope, maximum one-year expiry and immediate revocation. Personal Finance remains a separate highly sensitive, owner-private class rather than being confused with Wealth Tokens. Each class declares its sensitivity, purpose, examples, access model, retention behavior, and exact export/erase/revoke availability. The contract is available only to the authenticated owner through a private/no-store endpoint, is embedded in the portable account export, and appears in the existing Profile data section without adding a new primary-navigation surface. It explicitly identifies participant-shared records, restricted operational evidence, and external-provider records that LyfeOS cannot erase unilaterally. Account deletion waits for the PostgreSQL session-store destroy callback and both logout paths clear the configured `lyfeos.sid` cookie instead of the unused Express default. This contract remains factual product behavior, not approved legal language; deployed Clerk deletion, provider retention/deletion procedures, legal approval, and browser/assistive-technology comprehension remain external gates.

The consent-bound collaboration increment advances the clean schema to 128 raw migrations with `0136_consent_bound_collaboration.sql`. A workspace stores coordination purpose and explicit owner/coach/collaborator membership only. Invitees must accept inside their own authenticated account. Neither role nor workspace ownership can read Health, finance, relationships, journal, messages, AI memory, evidence, or any other personal record. An active member may expose only a personally owned Mission or Thread to another active member through an expiring summary/status projection; source descriptions, evidence, notes, progression internals and linked records are never copied. Membership leave/revocation atomically revokes every active projection for that recipient. Profile exposes creation, invitation, acceptance/decline, bounded sharing, received projections and immediate revocation without adding primary navigation. Collaboration metadata and audits are portable; account deletion cascades owned workspaces and subject grants while shared audit actor references become null. This foundation does not implement enterprise SSO, SCIM, billing, employment administration, or organization ownership of a person's record; those require a contracted identity provider and separate policy/authority decisions.

The AI execution and signed-extension increment adds release migration `0137_ai_execution_and_signed_extensions`. Every user selects local, hybrid, or cloud execution. Local means an installation-controlled OpenAI-compatible HTTPS endpoint configured by operations; it never accepts a browser-supplied endpoint or credential. Hybrid tries that endpoint first and reaches Anthropic only when the user explicitly enables cloud fallback. The effective policy is copied into a draft before approval and the resolution receipt records whether fallback was used, so changing settings cannot silently reroute an approved run. No available provider returns `503` before context is sent. Provider availability responses contain configuration state and model names, never keys.

The AI-memory lifecycle increment adds release migration `0138_ai_memory_lifecycle` and raises the clean ledger to 139 raw migrations and 130 release entries. Memory-policy writes now carry an optimistic revision and apply the selected purge inside the same transaction, preventing a stale tab from overwriting a newer policy or a failed purge from leaving a silently saved policy. Current text conversations, legacy messages, completed/cancelled voice sessions, context-source receipts and terminal governed-action receipts obey the selected retention windows. Active voice sessions and started, approval-pending or executing actions are retained. A database-advisory-lease sweep runs at startup and hourly, so retention is enforced without a Profile visit and multiple app instances cannot claim the same sweep. Explicit memory clearing is one transaction across the requested scopes; text and voice histories are both erased, resetting the AI identity clears the custom persona plus generated profile/affirmation, and action-history clearing returns the count of any in-flight receipt that remains. A fresh production-mode PostgreSQL 16 journey proved stale-write refusal, exact immediate and scheduled purge counts, active-record preservation, default persona reconstruction, account erasure, zero AI-memory residue, all 130 release entries and an exact second-run no-op. Required CI `33188633828` passed the full 835-test/build/audit lane and the raw-migration, authenticated-journey, release-convergence and isolated logical backup/restore lane for exact source `cf805db94c77571bb353a70a4a2f3277c97fe337`; documentation-inclusive source `dd7534eb32caa23e372427959a7ac227a5649ba7` repeated both lanes in required CI `33188902301`. Failed predecessor `33188414273` is not promotion evidence: it caught the backup/restore assertion still expecting 129 entries after every earlier step passed; source `cf805db94c77571bb353a70a4a2f3277c97fe337` corrected that invariant to 130 and passed it. Fly release 114 then promoted exact source `dd7534eb32caa23e372427959a7ac227a5649ba7` in image `deployment-01M14JF7HWXD6E46WK4HFWNX0J`; the release command completed successfully, machine `48ee093c323058` settled started with its HTTP check passing, `/api/ready` returned `ready`, `/api/release` returned the exact source/image, `Production`, and all 130 entries through `0138_ai_memory_lifecycle`, and anonymous `/api/auth/me` returned `401`. This qualifies exact migration, CI, deployment identity, readiness and public auth rejection; it does not substitute for an authenticated rendered memory-control journey or human mobile/assistive-technology comprehension.

The governed-action receipt hardening on 2026-08-28 qualified exact product source `3ac856800c60bcfdbd1d09b54905b05304e5501b` in required CI `33186055019`; documentation-inclusive source `188573aa66c1e73728753c61cdd9fa1ab5dfcc3a` then passed both required lanes in CI `33186279688`. Approval and decline now distinguish expired, unavailable, and execution-failed outcomes; an expired approval is finalized directly instead of remaining pending until a later list read; and pending-action plus durable-action receipt transitions commit atomically. The Profile surface invalidates pending/history caches after success or failure and refreshes every canonical domain that approval or repair can affect. A production-mode two-account PostgreSQL journey proved exact approved profile mutation and repair, atomic decline with no mutation, direct expiry with matching terminal receipts and no mutation, cross-account approval/decline denial, owner recovery of the still-pending decision, complete account cleanup, and zero residual users, pending actions, action records, or repair plans. Fly release 113 promoted exact source `188573aa66c1e73728753c61cdd9fa1ab5dfcc3a` in image `deployment-01M14GK0HXF8ZRCDQ8RSPXAE9B`; after promotion, Fly reported release status complete and machine `48ee093c323058` started at version 113 with one passing check, while fresh `/api/ready` and `/api/release` reads independently confirmed ready state, exact source/image, Production, and all 129 migrations through `0137_ai_execution_and_signed_extensions`; anonymous `/api/auth/me` returned `401`. This is exact CI/database/API/build/deployment-identity evidence, not a configured-model response, human browser comprehension, or authorization for external sending.

The extension registry accepts only strict `lyfeos.extension.v1` manifests signed with a trusted Ed25519 publisher key. Published slug/version pairs are immutable, requested permissions are limited to declared projection-read and draft-create capabilities, user grants must be a subset, and install/revoke actions are audited and portable. Publisher or package revocation disables affected installations. Extensions are manifests only: LyfeOS does not fetch or execute publisher JavaScript, binaries, prompts, or arbitrary network entrypoints in the application process. Registry administration is hidden and disabled unless an operations-owned token of at least 32 characters is configured. An empty catalog is a valid fail-closed state until OST approves a publisher, signing custody, package review, marketplace terms, and support ownership.

OST first-party signing custody uses publisher key `ost.first-party.2026`. Production stores only the public key and registry admin token. The Ed25519 private key and a matching registry token are write-only GitHub Actions secrets; the key ID is a non-secret repository variable. `.github/workflows/publish-extension.yml` is manual, serialized, production-environment scoped, read-only to repository contents, and accepts only a committed strict JSON manifest below `extensions/manifests`. The publisher script rejects traversal, non-JSON files, undeclared manifest fields, non-Ed25519 keys, redirects, invalid signatures, and duplicate immutable versions. No first-party or third-party package should be committed or published without product/security review of its requested projections and draft authorities.

The canonical Mission-source convergence increment adds no migration. The existing partial owner/lifecycle-key index is now represented consistently in the Drizzle schema and in the shared `ON CONFLICT` arbiter. Keyed Mission creation and its `mission_created` activity receipt commit in one PostgreSQL transaction; an exact retry returns the existing Mission without repeating automations or completed-at-create effects, while changed-payload key reuse fails closed. Stable source identities now cover Inbox capture and batches, Google Calendar/Tasks import, daily-log todo conversion, automation follow-ups, and durable AI action execution. Source-specific planning provenance no longer collapses Google, Inbox, or automation writes into `system`, and private lifecycle keys/hashes are removed from Inbox responses. On 2026-08-25, a fresh disposable PostgreSQL 16 environment applied all 116 raw migrations, reconciled all 107 release entries and produced an exact zero-output second pass. A production-mode three-test authenticated journey proved anonymous denial, one winner plus one exact replay from simultaneous Inbox writes, one Mission and one creation receipt, changed-payload conflict, ordered two-Mission batch replay without duplicate receipts, owner export, account erasure, and zero residual users, Missions, activity receipts, or sessions. The journey is now required by the PostgreSQL CI job. This is local database/API/build evidence, not remote CI, deployed immutable-source, live Google/AI/UMH-provider, or browser/device evidence.

The Mission current-path increment advances the clean schema to 117 raw migrations with `0116_mission_contract_method_pack.sql` and 108 release entries. Mission Contracts now persist bounded ordered method steps and tool requirements; canonical starter and skill-practice Missions receive an explicit default method pack. The existing Thread panel projects the recommended Mission's objective, method/tools, proof standard, support/review path and evidence-based advancement requirements without creating a parallel task authority or primary navigation surface. The wording explicitly separates reviewed LyfeOS practice evidence from certification, authority and personal worth. On 2026-08-25, a fresh disposable PostgreSQL 16 environment applied all 117 raw migrations, reconciled all 108 release entries and produced an exact zero-output second release pass. A production-mode authenticated seven-test API journey proved Thread initialization/activation, default method projection, custom method/tool persistence, immutable rubric review and appeal continuity, reviewed-capability reversal, portable export, account erasure, and zero residual users or sessions. Static wiring tests cover the protected Mission authoring/display and compact Thread projection. This is local database/API/build evidence, not proof that first-time users understand the path on real devices; deployed migration, reviewer notification/provider behavior, accessibility and observed usability remain separate gates.

The Mission prerequisite/unlock-result increment adds no schema migration. Existing owner-scoped Mission dependencies remain the canonical prerequisite authority; the completion route now preserves their intentional 409 response instead of flattening it into an internal error. `mission-reviewed-progression.v1` derives a read-only unlock result from the exact skill-contribution rows consumed by reviewed progression. It exposes the declared or applied reviewed-XP amount, skill and optional durable-capability rollup, reversal behavior, and explicit false certification/authority flags; it fails closed when no skill mapping exists and never accepts a free-form unlock promise. Mission Detail and the compact Thread current path show the same derived result. On 2026-08-25, a fresh disposable PostgreSQL 16 environment again applied all 117 raw migrations, reconciled all 108 release entries and produced an exact zero-output second release pass. The production-mode authenticated seven-test journey additionally proved cross-owner dependency denial, incomplete-prerequisite 409, prerequisite satisfaction, declared unlock projection, exact applied state after appeal reconsideration, return to declared state after reversal, account erasure, and zero residual users, sessions or dependencies. Unit tests cover fail-closed, declared and applied derivation. This is local API/build evidence; real-device comprehension, deployed immutable-source behavior and provider delivery remain separate gates.

The native reviewer-delivery increment advances the clean schema to 118 raw migrations with `0117_mission_review_native_delivery.sql` and 109 release entries. A Mission owner may select one existing LyfeOS reviewer from the existing username search; the resulting invitation is pre-bound to that authenticated user. In one transaction LyfeOS creates or reuses an eligible native direct conversation, commits a purpose-specific message whose extension contains only the bound invitation ID, records accepted/sent/delivered receipts and audit events, and marks the invitation `native_inbox`/`delivered`. The raw capability token is never stored in the message. A blocked, spammed, closed or otherwise unavailable recipient inbox aborts and rolls back the entire delivery, including revocation of the prior active invitation; the owner may still explicitly create the existing hash-only capability link, which has no delivery claim. On 2026-08-25, a fresh disposable PostgreSQL 16 environment applied all 118 raw migrations, reconciled all 109 release entries and produced an exact zero-output second release pass. A production-mode authenticated eight-test journey proved truthful manual-link state, blocked native delivery with atomic rollback, successful delivery after consent state was restored, owner denial, assigned-reviewer resolution, token-free message persistence, invitation acceptance, exact-rubric review and appeal, progression reversal, export, account erasure and zero residual users, sessions, invitations, conversations, messages or receipts. This is local database/API/build evidence. It does not prove rendered desktop/mobile comprehension, deployed migration/source identity, or external email/push delivery; those remain separate gates.

The Mission provider-provenance increment advances the clean schema to 119 raw migrations with `0118_mission_evidence_provider_provenance.sql` and 110 release entries. A provider evidence submission accepts only an owner-scoped active `health_source_records.id`; provider identity, record type, observation/receipt times, transform version and fingerprint are copied from the server-owned record into one immutable binding. Mission and reviewer responses expose only the safe provenance projection—never source payload, provider source ID or fingerprint. Review access remains hidden until an authorized reviewer accepts the scoped invitation. A later provider correction changes the projection to `superseded`; user deletion nulls the source reference and changes it to `source_deleted` while preserving the historical receipt and review context. Neither state silently reverses or awards progression. The owner export includes the binding receipt, and account erasure deletes it before Health source data. On 2026-08-25, a fresh disposable PostgreSQL 16 environment applied all 119 raw migrations, exposed 158 public tables, reconciled all 110 release entries and produced an exact zero-output second pass. A production-mode five-test journey across two temporary accounts proved provider-record listing isolation, rejection of client-forged metadata and cross-owner IDs, safe post-acceptance reviewer disclosure, `active`/`superseded`/`source_deleted` lifecycle truthfulness, export, erasure and zero residual users, sessions, bindings, evidence or source records. The full local gate passed 97 test files and 542 tests plus the production build and bundle budgets. This qualifies the LyfeOS boundary using seeded imported records; it is not evidence of a selected provider, live authorization, credential custody, synchronization, deployed migration, browser/device acceptance or production source identity.

The Thread durable-capability-focus increment advances the clean schema to 120 raw migrations with `0119_thread_capability_focus.sql` and 111 release entries. Each Thread may now identify one owner-scoped durable capability as its primary focus. The initial onboarding-derived focus is persisted explicitly, while a successor Thread can return to an existing capability without inheriting the prior Thread's missions, reviews, local XP or completion state. Capability history derives focus periods and the append-only progression/reversal ledger from the same local authority; its disclosure does not imply external certification. Initialization is serialized per user with a PostgreSQL advisory transaction lock so simultaneous requests converge on one current Thread. On 2026-08-25, a fresh disposable PostgreSQL 16 environment applied every raw migration, exposed 158 public tables after release bookkeeping, reconciled all 111 entries and produced an exact zero-output second release pass. A production-mode five-test authenticated journey across two temporary owners proved foreign-capability denial, earning and linked reversal history, concurrent successor convergence, fresh local XP beside retained durable XP, two focus periods, export, exact erasure and zero residual users, sessions, Threads, capabilities or progression events. The exact container was removed. The complete source gate also passed 98 test files and 545 tests, the production build and all bundle budgets. This is local migration/API/build evidence, not remote CI, deployed immutable-source behavior, browser/device accessibility or longitudinal product-effectiveness evidence.

The Mission consequence-preflight increment advances the clean schema to 121 raw migrations with `0120_mission_consequence_preflight.sql` and 112 release entries. High-risk Mission Contracts carry a material contract revision and remain draft until an owner records an append-only same-revision preflight and explicitly accepts a `proceed` decision. Expected, upside and downside scenarios retain early signals without fabricated likelihood scores; the receipt also retains assumptions, affected parties, reversibility, mitigation, uncertainty, decision rationale, the server-snapshotted stop conditions and an explicit no-authority acknowledgement. `revise` and `do_not_proceed` remain blocking decisions. Any material proof-plan edit or review-authority change increments the revision and makes earlier receipts stale. Contract edits, preflight recording, acceptance, review-authority changes and the shared completion lifecycle serialize on the same Mission-scoped database lock, so concurrent material edits receive distinct revisions and a stale approval cannot win a completion race. The shared Mission completion lifecycle refuses a high-risk completion without a current accepted proceed receipt regardless of whether UI, AI or another adapter invoked it. On 2026-08-25, a fresh disposable PostgreSQL 16 environment applied all 121 raw migrations, replayed `0120` safely, exposed 159 public tables after release bookkeeping, reconciled all 112 entries and produced an exact zero-output second release pass. A production-mode six-test authenticated journey across two temporary owners proved ownership isolation, schema rejection, revise refusal, proceed acceptance, concurrent revision serialization, content and review-authority revision invalidation, canonical completion enforcement, export and account erasure with zero residual users, sessions, Missions, contracts or preflights. The exact container was removed. The complete source gate passed 99 test files and 548 tests, the production build and all bundle budgets. This is local database/API/build evidence; it does not establish real-world safety, professional advice, deployed immutable-source behavior, remote CI, browser/device comprehension or product-effectiveness evidence.

The Health insight-interpretation increment advances the clean schema to 122 raw migrations with `0121_health_insight_interpretations.sql` and 113 release entries. Explicitly requested available associations return a leave-one-day-out sensitivity range beside the existing coefficient, sample, coverage, missingness, alignment and confounder disclosures. A source-led review against NIST's independent-observation Fisher approximation and published repeated-measures guidance rejected the earlier Fisher-z interval for this single-person daily context: consecutive records may be serially dependent. The replacement recalculates Pearson's coefficient after omitting each paired day and explicitly states that its range is neither a confidence interval nor a probability statement and does not correct serial dependence or establish causation, prediction, diagnosis or source accuracy. Users can save one of three private interpretations only after acknowledging that exploratory boundary. The server recomputes the current selected association, refuses insufficient evidence, persists calculation/quality metadata without aligned daily values, converges exact mutation retries, rejects changed identity reuse, and keeps the record outside Health progression. Listing is private/no-store; item deletion, portable account export and full Health-domain erasure include the record. On 2026-08-25, a fresh PostgreSQL 16 environment applied all 122 raw migrations, exposed 160 public tables, reconciled all 113 release entries and produced an exact zero-output second pass. A production-mode four-test journey across two temporary owners proved anonymous denial, owner isolation, sensitivity output, sparse refusal, metadata-only snapshotting, replay/conflict, export, Health-domain erasure and zero residual users, interpretations, source records or sessions. The exact container was removed. The complete source gate passed 100 test files and 551 tests, the production build and bundle budgets. This is source-led analytic-contract/API/build evidence; it is not independent statistician sign-off, clinical evidence, deployed-source proof or browser/device comprehension evidence.

On 2026-08-25, a disposable PostgreSQL 16 environment applied all 112 raw migrations through `0111`, exposed 155 public tables, reconciled all 103 release-runner entries and produced an exact zero-output second pass. A five-test production-mode authenticated journey across two temporary owners proved cross-owner Table denial; canonical relation creation; derived backlinks; deletion refusal before unlink; unlink as source-row version two with both immutable revisions; conditional hidden-field rejection; accepted owner submission; a 256-bit external secret absent from owner responses; exactly one winner from two simultaneous submissions competing for a one-response grant; revocation; and account erasure leaving zero users, grants and receipts. The journey is now part of the required PostgreSQL CI job. The server and disposable container were removed afterward. This is local clean-room API evidence, not remote CI, deployed migration, production traffic, large-account performance, or browser/device accessibility evidence.

The product-analytics consent increment advances the clean schema to 124 raw migrations with `0123_product_analytics_consent.sql` and 115 release entries. A fresh disposable PostgreSQL 16 container applied every raw migration, exposed 162 public tables, accepted all release entries, and produced an exact zero-output second release pass; the container was removed. The source gate passed 104 active test files and 573 tests, the production client/server/release build, bundle budgets and a zero-advisory production dependency audit. A separate six-test production-mode authenticated journey proved anonymous denial, private/no-store default-off status, fail-closed enablement without complete provider deletion controls, idempotent disabled consent, rights/export coverage, account erasure and session invalidation. Product analytics remains off by default and unavailable unless the PostHog project token/ingestion host plus server-only project ID/personal API key/admin host are complete. The adapter permits nine content-free lifecycle events, disables autocapture, replay, exceptions, surveys and browser feature flags, and uses one-use random subjects. Withdrawal/account deletion queues exact-subject person and event deletion after a 15-minute ingestion safety window. A later provider-preflight increment also denies opt-in unless a live project read verifies project identity, IP discard, disabled invasive capture defaults, and explicit retention enforcement; users with prior consent can still revoke while provider capture is paused. This is local schema, authenticated API, contract, test and build evidence only; no production event, external PostHog deletion receipt, dashboard or approved privacy language is implied.

The Mission planning-context amendment increment advances the clean schema to 125 raw migrations with `0124_mission_planning_context_amendments.sql` and 116 release entries. A fresh disposable PostgreSQL 16 container applied every raw migration, exposed 163 public tables, accepted all release entries and produced an exact zero-output second release pass; the container was removed. The source gate passed 105 active test files and 576 tests plus the production build and bundle budgets. A three-test production-mode authenticated journey proved owner-only source disclosure, two append-only full-snapshot corrections, exact previous-snapshot continuity, stale-revision conflict, an unchanged creation snapshot and cascade erasure. This is local migration, authenticated API and build evidence; remote CI, production migration, deployed source and real-device comprehension remain separate gates.

The cross-domain hypothesis increment advances the clean schema to 126 raw migrations with `0125_cross_domain_hypotheses.sql` and 117 release entries. A fresh disposable PostgreSQL 16 container applied every raw migration, exposed 167 public tables, accepted all release entries and produced an exact zero-output second release pass; the exact container was removed. The complete source gate passed 106 active test files and 579 tests, the production build and bundle budgets. A four-test production-mode journey across two temporary owners proved default-off per-domain consent, refusal before consent, an available Mission/Health comparison at the seven-pair and 20%-coverage boundary, owner isolation, omission of aligned daily values, provenance and zero/unknown semantics, idempotent private interpretation with changed-payload refusal, zero progression side effects, portable account and Health exports, Health-derived hypothesis erasure, consent revocation and zero residual users, hypotheses, snapshots or interpretations. The scheduled worker recalculates active hypotheses at most daily and records recoverable failure state; it does not create Missions, award XP or make recommendations. This is local schema, API, analytic-contract and build evidence, not independent statistical review, remote CI, production migration/deployment, cross-product adapter proof, or browser/device comprehension evidence.

The scheduled-automation increment advances the clean schema to 127 raw migrations with `0126_scheduled_workflow_automations.sql` and 118 release entries. A fresh disposable PostgreSQL 16 container applied every raw migration, exposed 167 public tables, confirmed all three schedule instant columns as `timestamp with time zone`, accepted every release entry and produced an exact zero-output second release pass. The authenticated four-test journey proved disabled-by-default owner-scoped schedules, foreign-anchor refusal, one winner under concurrent claims, bounded run-once consolidation, missed-run skip without Mission mutation, deterministic follow-up dates based on the scheduled local day, portable export, account erasure, and zero residual users, automations or runs. The exact container was removed. The complete source gate passed 107 active files and 583 tests with 30 provider/database files explicitly gated, followed by the production client/server/release build and all bundle budgets. The worker does not run in isolated API-test servers. This is local schema/API/build evidence, not remote CI, production migration, repeated-failure alert delivery, deployed immutable-source behavior, or browser/mobile/assistive-technology acceptance.

The application emits structured request logs with `x-request-id`; Fly remains the live source for runtime logs and machine health. Sentry is initialized only when `SENTRY_DSN` is present. Its React initializer runs before the application, captures browser errors, and traces page/API activity; the server captures Express errors, uncaught exceptions, unhandled rejections, the manual protected observability probe, and the exact third-failure transition that safety-pauses an automation. Runtime operational events contain no user identity, rule/mission text, or action payloads. Set `SENTRY_ENVIRONMENT=production`; the immutable `LYFEOS_RELEASE` baked into each deployed image is automatically used for both server and browser Sentry attribution, while `SENTRY_RELEASE` is only a non-image fallback. The client disables automatic user, cookie, header, query, body, AI input/output, and local-variable collection. The server strips authorization/cookie headers before an event is sent. Session replay is intentionally deferred pending an explicit consent and retention decision. The DSN, production environment and fallback release secret names are deployed in Fly; a successful manual probe proves deployed SDK transport only, while provider receipt and routed-alert evidence must still be confirmed in Sentry before calling the alert path live. Medium-risk assistant mutations enter a 15-minute user approval queue; expired/declined actions must leave an activity receipt and make no domain change.

The UMH outbox-leasing increment advances the clean schema to 129 raw migrations and 120 release entries through `0128_umh_outbox_leases`. The prior process-local delivery guard is replaced by a PostgreSQL lease state machine: due rows are claimed in disjoint batches with `FOR UPDATE SKIP LOCKED`, attempts increment on claim, expired processing work is recoverable, and every delivered/retry/failed settlement requires the current lease token. Retry, creation and delivery timestamps are converted from legacy UTC `timestamp` values to timezone-aware instants. Lease credentials are ephemeral and excluded from account export. A clean PostgreSQL 16 rehearsal applied all raw migrations, reconciled all release entries to an exact second-run no-op, and proved concurrent command convergence, conflicting idempotency refusal, one canonical Mission/outbox result, multi-worker claim isolation, stale-worker refusal, bounded retry/terminal errors and expired-lease recovery. The first production release attempt safely stopped before promotion because the older release path had never created the raw-only UMH baseline. Migration `0128` was made self-healing for that exact drift. Exact CI run `32986816597` deliberately removed all five UMH tables, reconstructed them through the release runner, passed the federation conformance journey and restored a structurally identical logical backup. Production application revision `58c95a12427c72b04744778b87fa7301e7972316` then applied `0128`; deployment `deployment-01M0ZD8MW52DAVPW1EDBGT5WRE` is ready and `/api/release` reports 120 entries with `0128` latest. This is LyfeOS-side database and deployment conformance; a configured UMH receiver and signed round trip remain separate external evidence.

On 2026-08-28, exact runtime commit `517d0bb6d6b9c0a5c1ae279fd3034e1793f006d1` passed all 829 local tests across 168 files, typecheck, production build and bundle budgets. Required CI `33154662150` passed the full verification/security lane plus complete raw-migration replay, release convergence, authenticated product and recovery journeys, and isolated logical backup/restore. Fly release 104 promoted image `deployment-01M13Q5P3DEXZJ6S8VZFQGXRQ7`; the machine check and `/api/ready` returned ready, while `/api/release` reported the exact source and all 129 release entries through `0137_ai_execution_and_signed_extensions`. The acceptance control plane records runtime and harness identity separately; exact harness/custody commit `cf6a76264e9da0a1660f728b922f0045990075c3` passed required CI `33157537607`. Protected production acceptance `33157717159` then retained 84/84 passing public/protected desktop/mobile route cases and the complete rendered core-loop report for that runtime/harness pair. Its dedicated authenticated account created synthetic Mission 34 through the UI, revised and persisted the automatic purpose, method, observable output, weighted evidence rubric, low-risk choice and self-review boundary, then attached an unreviewed artifact. The settled baseline was 625 activity XP and 0 capability XP; unreviewed evidence changed neither track and granted no badge. Timer-backed rendered completion awarded exactly 10 activity XP and the supported `first-real-action` badge while capability XP remained withheld. Positive rendered self-review then awarded exactly 10 capability XP without certification or entrusted authority. The Experience view rendered 635 activity XP, a bounded reversal-aware ledger whose ending cumulative total reconciled exactly to 635, 10 reviewed capability XP, and the certification/authority boundary. Rendered Undo restored the exact 625/0 baseline, removed the supported badge, and returned the reviewed skill to declared-only state. Both desktop and mobile Mission views had one main landmark, nine headings, no unnamed controls and zero horizontal overflow. Cleanup archived the synthetic Mission through the recoverable canonical route and left the progression snapshot unchanged. This qualifies the deployed self-reviewed Mission path from revisable planning through evidence, completion, review, explainable two-track progression visualization and reversal. It does not substitute for observed human keyboard/screen-reader comprehension, real-device offline/reconnect behavior, provider routing, real-field performance or longitudinal effectiveness.

The next exact promotion on 2026-08-28 moved runtime and harness source `299c8a39ecdf413b918159b23003c7529c558955` through required CI `33159353559`; both the full 829-test/build/security lane and the complete raw-migration, authenticated-journey, release-convergence and isolated backup/restore lane passed. Fly release 105 promoted image `deployment-01M13V6YKM4YJFV5HPVMDEZRAT`; `/api/ready`, the machine health check, and `/api/release` independently confirmed healthy state, the exact source, Production environment and all 129 release entries through `0137_ai_execution_and_signed_extensions`. Protected production run `33159772376` retained 84/84 passing route-viewport cases and `lyfeos.production-core-loop-acceptance.v3`. The dedicated account created synthetic Mission 35 from the rendered UI and again proved the 625/0 baseline, no progression from unreviewed evidence, exact 635/0 timer-backed completion, exact 635/10 positive self-review and no certification or entrusted authority. The new dashboard check reconciled its canonical current path to `/api/transformation-thread`, rendered all five current capability nodes, showed capability 1 at 10 reviewed XP and focus count one, and rendered reviewed event 17 at +10 XP. The Experience ledger reconciled exactly. Rendered Undo restored the exact 625/0 progression snapshot, and the dashboard then showed capability event 18 at -10 XP with `reversalOfId` 17 and the durable total back at zero. Cleanup archived Mission 35. This proves current-focus rendering and exact earning/reversal continuity in one deployed release; it does not prove a successor focus across releases, first-time human comprehension, longitudinal usefulness or external-provider behavior.

The bounded-automation production promotion on 2026-08-28 qualified application source `eb44eafc63727c276ac448b955dafe7ca256fd3c` in required CI `33162704903`; both the complete 830-test/build/security lane and the raw-migration, authenticated-journey, release-convergence and isolated logical backup/restore lane passed. Fly release 107 promoted image `deployment-01M13Y27DMDVG5HY7WPQ0RXMWN`; a fresh read of `/api/ready` returned ready and `/api/release` returned that exact source, image, Production environment and all 129 release entries through `0137_ai_execution_and_signed_extensions`. Acceptance-harness source `d3bd5ea40cd85e8a6d968394ea76ffe0b8b700ed` independently passed both required CI lanes in `33168088641`. Protected production run `33168276506` then retained 84/84 passing route-viewport cases: 16 public and 68 authenticated across 42 desktop and 42 responsive cases, with zero reported route failures, failed requests, server errors, console errors, horizontal overflow, duplicate IDs or unlabeled controls. Its `lyfeos.production-core-loop-acceptance.v4` report created one synthetic Mission and one disabled bounded automation through rendered controls. Preview matched the Mission and disclosed `Preview only. No mission was changed and no follow-up was created.`; it retained zero run receipts, created no follow-up Mission and left progression unchanged before the rendered automation was deleted. Unreviewed evidence again preserved the settled 625 activity/0 capability baseline. Timer-backed completion applied exactly +10 activity XP, positive self-review applied exactly +10 reviewed capability XP, and neither granted certification or entrusted authority. The dashboard rendered the five-node capability graph, current path and linked +10 review event; rendered Undo restored the exact 625/0 baseline and linked -10 reversal. Cleanup deleted the automation, archived the Mission and preserved the baseline. This qualifies saved disabled automation creation, persistence, non-mutating preview and deletion in the deployed UI. It does not qualify actual manual/scheduled execution, concurrent run/repair UI, alert-provider routing, human assistive-technology comprehension, real-device behavior or longitudinal effectiveness.

The rendered automation-control promotion on 2026-08-28 qualified source `9ff07e68b9e69771a587922033eefa0117a413c9` in both required CI lanes in run `33169755328`; the complete 830-test/build/security lane and the raw-migration, authenticated-journey, release-convergence and isolated logical backup/restore lane passed. Fly release 108 promoted image `deployment-01M144CVTGRYS2Q445DFZEP0W4`; Fly reported one started machine with its one check passing, `/api/ready` returned ready, and `/api/release` returned the exact source, image, Production environment and all 129 release entries through `0137_ai_execution_and_signed_extensions`. Protected production run `33170138730` independently verified the same runtime and harness source before retaining 84/84 passing route-viewport cases: 16 public and 68 authenticated across 42 desktop and 42 responsive cases, with zero route failures, failed requests, server errors, console errors, horizontal overflow over two pixels, duplicate IDs or unlabeled controls. Its `lyfeos.production-core-loop-acceptance.v5` report saved one bounded manual rule through rendered controls, matched the synthetic Mission in Preview, rendered `Preview only. No mission was changed and no follow-up was created.`, enabled the rule, proved Run now was available while enabled, paused the rule, and deleted it. The entire control cycle retained zero run receipts, created no follow-up Mission and left progression unchanged. The Mission journey again preserved 625 activity/0 capability XP before review, applied exact +10 activity XP on timer-backed completion and exact +10 reviewed capability XP only after positive self-review, granted no certification or entrusted authority, rendered the five-node capability graph and linked +10 event, then restored the exact 625/0 baseline and linked -10 reversal through Undo. Cleanup deleted automation 8, archived Mission 44 and preserved the baseline. This qualifies rendered save, preview, enable, Run-now availability, pause and deletion with zero execution side effects. It deliberately does not activate Run now, because its append-only production run receipt cannot be removed from the dedicated account; actual manual/scheduled execution, concurrent run/repair UI, alert-provider routing, human assistive-technology comprehension, real-device behavior and longitudinal effectiveness remain separate evidence gates.

The rendered schedule-authoring promotion on 2026-08-28 qualified application source `d082bd2548e895f41ce609c301cab22226306c8e` in both required CI lanes in run `33173965273`; all 830 tests, production builds, dependency audit, raw-migration replay, release convergence, authenticated journeys and isolated logical backup/restore passed. Fly release 110 promoted image `deployment-01M147W659EW93KB3CZ6X3T86K`; Fly reported one started machine with its check passing, while `/api/ready` returned ready and `/api/release` independently returned the exact source, image, Production environment and all 129 release entries through `0137_ai_execution_and_signed_extensions`. Acceptance-harness source `1e7e4d130916c620458a73ffb6faecc3e852ccae` passed both required CI lanes in run `33175420250`. Protected production run `33175575857` then rechecked the runtime/harness identities and retained 84/84 passing route-viewport cases: 16 public and 68 authenticated across 42 desktop and 42 mobile cases, with zero route failures, failed requests, server errors, console errors, overflow over two pixels, duplicate IDs or unlabeled controls. Its passing `lyfeos.production-core-loop-acceptance.v6` report created Mission 48 and automation 12 through rendered controls. The manual rule was saved, previewed with the exact non-mutation disclosure, enabled, shown as Run-now eligible, and paused. The paused rule was converted to a disabled daily schedule, bounded to two occurrences with missed runs skipped, then revised to a disabled UTC weekly schedule at 09:00 on weekdays 5 and 6, starting 2026-08-28, bounded to three occurrences with `run_once`; its next occurrence was 2026-08-29T09:00:00Z and Run now remained unavailable. A second preview still produced zero run receipts, no follow-up Mission and no progression change. Unreviewed evidence preserved the 625/0 baseline; timer-backed completion applied exact 635/0 plus `first-real-action`; positive self-review applied exact 635/10 without certification or authority; rendered Undo restored exact 625/0 and removed the supported badge. Cleanup deleted automation 12, archived Mission 48 and preserved the baseline. This qualifies rendered schedule creation, revision, future-occurrence presentation and non-executing preview. It does not qualify an actual scheduled worker execution, manual Run now, concurrent failed-run repair UI, provider alert delivery, human assistive technology, real devices or longitudinal outcomes.

The truthful automation-receipt promotion on 2026-08-28 qualified source `0906702e30a99bf1868d543a2b835f4c20bf722a` in both required lanes in CI `33177640480`, including the complete 830-test/build/audit gate, all authenticated disposable-database journeys, raw and release migration convergence, and isolated logical backup/restore. Fly release 111 promoted image `deployment-01M14AKVR2W0RC21J6XF32NC96`; Fly reported one started machine and one passing check, while fresh `/api/ready` and `/api/release` reads independently confirmed ready state, the exact source and image, Production environment and all 129 release entries through `0137_ai_execution_and_signed_extensions`. Protected production run `33178093136` used that same source for runtime and harness, retained 84/84 passing route-viewport cases—16 public and 68 authenticated across 42 desktop and 42 mobile cases—and reported no route failures, failed requests, server errors, console errors, overflow over two pixels, duplicate IDs or unlabeled controls. Its passing `lyfeos.production-core-loop-acceptance.v6` journey created Mission 49 and automation 13, repeated the non-executing manual and disabled schedule-control cycle with zero run receipts, follow-up Missions or progression mutation, applied and exactly reversed the 625/0 to 635/10 reviewed progression, granted no authority, deleted the automation, archived the Mission and restored the exact baseline. This qualifies the deployed recovery-receipt implementation and broad empty-history route rendering, not an actual production run or rendered failed-repair state.

The isolated rendered-recovery qualification on 2026-08-28 passed required CI `33182386086` for source `ba318979447deedbcb3cd4439bec1035b52f6c7d`. Both the complete 832-test/build/audit lane and the raw-migration, authenticated-journey, release-convergence, Chromium and isolated logical backup/restore lane passed. Its uploaded `lyfeos.isolated-automation-recovery-browser.v1` report created a disposable local-session account, Mission, manual automation and two-action execution in isolated PostgreSQL; rendered the same partial receipt at 1440x900 and 390x844 with ordered actions, bounded record IDs, one attempt each, a human-readable generic failure, an explicitly named Repair control, no copied private Mission description, duplicate IDs, unlabeled controls or horizontal overflow; activated Repair; proved the successful action stayed at one attempt while only the failed action advanced to two; retained exactly one lifecycle-keyed follow-up Mission; removed Repair after success; and erased the account with zero residue. This closes the disposable rendered partial-run repair gate. It is not production execution, scheduled-worker evidence, a human assistive-technology review, or Sentry provider alert-routing evidence. At the time of this qualification, production was still Fly release 111 source `0906702e30a99bf1868d543a2b835f4c20bf722a`; the later promotion record below supersedes that snapshot.

The isolated scheduled-receipt qualification on 2026-08-28 passed required CI `33183869004` for exact source `da000c8a6ba0bae11e37d6b46d558fa52746e224`. Both the complete 832-test/build/audit lane and the raw-migration, authenticated-journey, release-convergence, Chromium and isolated logical backup/restore lane passed. Its uploaded `lyfeos.isolated-automation-recovery-browser.v2` report retained the v1 manual repair proof, then invoked the real scheduled processor twice against one due rule: one claimant returned `completed`, the other `busy`, two due occurrences consolidated into one succeeded receipt, and exactly one keyed follow-up Mission was created. Desktop 1440x900 and mobile 390x844 rendered the scheduled context, local date, UTC zone, delayed/consolidated disclosure, action outcome and bounded Mission ID. Separate bounded display fixtures proved failed and running receipts, including completed/not-completed semantics, human-readable generic failure, state-aware Retry/Check labels and the live-work recovery-window disclosure. All nine views reported zero copied private descriptions, duplicate IDs, unlabeled controls and horizontal overflow; cleanup left zero users, automations, runs or action receipts. The scheduled run used the real worker and canonical action lifecycle; failed/running rows were display fixtures only. This is not production execution, human assistive-technology review or Sentry provider alert-routing evidence.

Fly release 112 promoted exact source `2b82e519a9ba9d670cbdc017eb2ac12c8e74bb25`, including the v2 product/harness commit and its evidence documentation, in image `deployment-01M14F5X5N1W71N4C2VA3K81XJ`. Required CI `33184240686` passed both lanes for that exact source before promotion. After promotion, Fly reported release status `complete`, machine `48ee093c323058` started at version 112 with one passing check; fresh `/api/ready` returned `ready`; `/api/release` independently returned the exact source, exact registry image, `Production`, and all 129 migration entries through `0137_ai_execution_and_signed_extensions`; anonymous `/api/auth/me` returned `401`. These checks qualify deployment identity and the public authentication boundary, not a production automation execution, provider receipt, or human assistive-technology review.

## Backup and restore

Neon is the authoritative recovery provider. Its branch history supports point-in-time recovery; retain at least seven days of history (or 30 days on a plan that supports it). Before changing schema or destructive data, create a Neon snapshot and record its timestamp and restore point in the release notes. At least quarterly, create an isolated branch from a snapshot or a point in time and verify: schema, one login, one account creation, and one mission read/write. Delete the temporary branch after the evidence is recorded. Never run a restore against production without an explicit change window and approval.

On 2026-08-26, an isolated non-primary Neon branch was created from the production project at `2026-08-26T19:06:22Z`. Its user count, mission count, release-migration count and latest ID, public-table count, and encrypted provider-credential count exactly matched production. The branch identity and non-primary/non-default status were rechecked before deletion, and the production project was then confirmed to contain only its Production branch. The restore window was raised from 21,600 seconds to the seven-day target of 604,800 seconds, and the provider's storage-configuration operation finished. A second isolated branch restored production to `2026-08-26T19:41:48Z`; a production-mode server passed 17 account registration/login/session/logout/deletion and mission/Project read/write/recovery tests, and cleanup returned its three-user baseline to exactly three. The branch was revalidated as the bounded non-primary/non-default target, deleted, and Production was left as the sole branch. This qualifies the managed PITR mechanism, target window, application journey, data cleanup and provider cleanup; it does not establish production-sized recovery timing or an organization incident rehearsal.

Every required PostgreSQL CI journey also creates a custom-format logical dump after the authenticated suite, restores it into an isolated second database, and compares one deterministic fingerprint over all public columns/defaults, constraints and indexes plus the complete ordered release-migration ledger. This continuously qualifies application-controlled logical backup/restore and detects structurally incomplete restores. It does not replace the quarterly managed Neon point-in-time branch drill, provider retention verification, or an authorized login/read/write rehearsal against that restored branch.

### Release 115 — durable AI-memory reset and rendered lifecycle evidence

Current source contains 140 raw migrations and 131 release-runner entries through `0139_affirmation_reset_choice`; the suite contains 836 tests across 168 files. Migration `0139` records a reset user’s explicit choice not to auto-regenerate an affirmation until they later request or save one. The isolated browser harness uses only localhost, a disposable PostgreSQL database, synthetic records, and production-bundle Chromium. At desktop 1440×900 and mobile 390×844 it exercises all three retention selectors, chat/profile/context/action clearing, active-action disclosure, persona reset, database state, account erasure, and residue checks while also rejecting duplicate IDs, unnamed controls, overflow, unexpected console errors, and failed same-origin requests.

Exact CI `33192053274` qualified product source `2e40c5e1785ad45f4b05b241676bbe22882f4720` and retained SHA-bound automation and AI-memory browser artifacts. Exact CI `33192304238` repeated the full verification and PostgreSQL lane for deployment source `30c8ad967cd93d74f079baad350640c7f0305a18` after local acceptance directories were excluded from the Docker context. Fly release 115 then ran the release command and promoted image `deployment-01M14MYBZEDP9QF4XDW05QWJ6G`. Fresh production checks returned `ready`, exact source and image, `Production`, 131 migrations through `0139_affirmation_reset_choice`, one started machine with a passing HTTP check, and `401` from anonymous `/api/auth/me`. Independent production monitor `33192773427` passed readiness, anonymous-auth, immutable release/migration identity, and protected aggregate Health integrity. This does not substitute for a dedicated authenticated production-browser memory journey or human mobile/assistive-technology comprehension.

### Release 116 — privacy-gated product-analytics foundation

Exact CI `33194212904` qualified source `50ecc28b1d0bec17f35ff64f648c269f823d8725` in both required lanes before Fly release 116 promoted image `deployment-01M14PAMTA75B4D792TVQ8WY7X` to machine `48ee093c323058`. Fresh checks returned `ready`, exact immutable source and image identity, `Production`, all 131 release migrations through `0139_affirmation_reset_choice`, and `401` from anonymous `/api/auth/me`; independent monitor `33194677618` passed the production health, release-identity, anonymous-auth and aggregate Health-integrity checks. The release added a fail-closed live PostHog project preflight and preserved withdrawal when the provider is unavailable or drifts. It did not activate production analytics: Fly still lacked the complete five-value configuration, and the provider audit found client-IP discard disabled, invasive collection defaults enabled and retention not explicitly enforced. No production event or provider deletion receipt is claimed.

### Isolated successor-Thread continuity evidence

Exact CI `33196333430` qualified source `30b70b4650222619e6f12d48a2f582ab4fb0e01e` in both required lanes. The complete 844-test/build/critical-audit lane passed, and the PostgreSQL lane replayed all raw migrations, converged all 131 release migrations, passed the authenticated integration suite, retained the existing automation-recovery and AI-memory artifacts, uploaded `lyfeos.isolated-thread-successor-browser.v1`, and completed the deterministic logical backup/isolated restore rehearsal.

For each of desktop 1440×900 and mobile 390×844, the successor artifact registered a disposable owner, completed onboarding, initialized and activated a first Thread, created exactly three canonical starter Missions, added one bounded artifact to each declared proof plan, completed and positively reviewed all three against their exact rubric, recorded two Thread reviews, and called the real Thread completion route after backdating only the activation timestamp beyond the 28-day minimum. It then used the rendered dashboard selector and activation controls to create a distinct second focus over the same durable capability. Both views rendered the completed first focus and active successor separately, retained 30 durable reviewed XP, showed zero Thread-local successor XP, and reported a focus count of two with zero duplicate IDs, unlabeled controls, horizontal overflow, same-origin server failures or unexpected console errors. Account erasure left zero audited user, Thread, Thread-evidence, capability, skill, edge, Mission-link, progression-event, Mission, contract, evidence, review or widget-state rows. Isolated local Clerk-resource DNS errors were recorded separately and did not conceal application errors. This evidence proves exact-source disposable continuity; it does not yet prove production-account persistence across a later release, longitudinal usefulness, external certification/authority or human assistive-technology comprehension.

### Release 117 — successor-Thread continuity promotion

Required CI `33196788757` repeated both required lanes on exact source `325c92f732bee319a166b45ad64a5a51d3b0c7d1`, including the 844-test/build/audit gate, authenticated PostgreSQL journeys, all three disposable browser artifacts and logical backup/isolated restore. Fly release 117 ran the release command and promoted image `deployment-01M14R7ZASBBX35VPDPQ0WVVJJ` to machine `48ee093c323058`. Fresh LyfeOS.net checks returned `ready`, exact source and image, `Production`, 131 migrations through `0139_affirmation_reset_choice`, a started machine with its HTTP check passing, and `401` from anonymous `/api/auth/me`. Independent production monitor `33197237302` passed readiness, anonymous-auth, immutable release/migration identity and protected aggregate Health integrity. This deploys the successor-capable implementation; the completed-to-successor acceptance itself remains disposable exact-source CI evidence, not an authenticated production-account claim.

Protected production-browser run `33198882514` then bound deployed source `325c92f732bee319a166b45ad64a5a51d3b0c7d1` to repaired harness source `ff7c16b5d3499f72c94c3d1054ff31b22b545246`. Its broad report passed all 84 public and authenticated route/viewport cases at desktop 1440×900 and mobile 390×844 with zero route failures, server errors, failed requests, duplicate IDs, unlabeled controls or horizontal overflow. Contract `lyfeos.production-core-loop-acceptance.v7` used the dedicated completed-onboarding account to create one rendered skill-linked Mission, persist its proof plan, add an artifact without progression, exercise non-mutating automation preview/enable/pause and disabled schedule authoring/revision, complete through the rendered timer, and self-review the exact evidence. Activity moved exactly 625→635 only on completion; reviewed capability moved exactly 0→10 only after review; certification and authority stayed empty; rendered Undo restored the exact 625/0/no-badge baseline and added the linked -10 capability reversal. The rendered dashboard reconciled its five-node graph, current path and focus history to the same API state. Cleanup deleted automation 15 and archived synthetic Mission 51 through the canonical recoverable route. Because the acceptance account is durable, its append-only review/reversal and archived-Mission history intentionally remain; the evidence proves exact progression restoration and active-automation cleanup, not zero historical rows or a disposable-account erasure.

### Provider-independent Mission-source convergence qualification

Exact CI `33199920290` passed both required lanes for source `559a0b508a00ed4ae57ae9f1ad4585ba7ea91a61`. The source gate passed TypeScript, all local tests, production build, bundle budgets and the critical production dependency audit. The PostgreSQL lane replayed every raw migration, converged all 131 release entries to an exact second-run no-op, passed the complete authenticated suite, retained all three disposable browser artifacts, and matched the isolated logical restore. Its expanded Mission-source journey proved UI mutation replay, onboarding deduplication, automatic prior-day To-Do conversion with explicit `todo` rather than ambiguous `system` provenance, an explicitly approved keyed AI Mission, duplicate-safe automation follow-up creation, one transaction-bound Thread starter set, exact per-source `mission_created` receipts, export and complete account erasure. Google provider behavior and signed UMH HTTP ingress remain separate live/deployed gates; the CI journey does not claim either.

### Release 118 — truthful Mission-source provenance promotion

Documentation-inclusive exact CI `33200152823` passed both required lanes for source `a77618db7929c33553da5639df803c317a1bf3b7`, repeating the 847-test source gate and the PostgreSQL raw-migration, authenticated-journey, 131-entry release-convergence, browser-artifact and isolated logical-restore lane. Fly release 118 ran the release command successfully and promoted image `deployment-01M14TRF5RQY974NGRE0WMEBYS` to machine `48ee093c323058`. Fresh checks returned `ready`, exact source and image, `Production`, 131 migrations through `0139_affirmation_reset_choice`, one started version-118 machine with its HTTP check passing, and `401` from anonymous `/api/auth/me`. Independent production monitor `33200606928` then passed readiness, anonymous-auth isolation, immutable release/migration identity and protected aggregate Health integrity. This promotes the provider-independent source-convergence implementation; it does not claim a live Google import, signed UMH receiver round trip, or a fresh production mutation for every local source.

### Releases 123–128 — complete rendered onboarding lifecycle and connected capability focus

The rendered onboarding qualification loop kept runtime, harness, and provider evidence separate. Release 123 promoted source `f3b947ef` after bounding client-session synchronization; the real onboarding surface mounted, exposing a stale preloader. Release 124 promoted `402aeb0f` and removed that reload obstruction; acceptance then exposed overlapping Mission completion and Thread activation. Release 125 promoted `e7e4b13f` and serialized all eight onboarding Mission completions before Thread activation. Its desktop journey completed every Mission, produced an active Thread, and preserved the session, but correctly refused to claim rendered account erasure because the acceptance marker was not attached to the real deletion field. Release 126 was an explicitly non-certifying deployment: its build omitted the immutable release argument and reported stale source `2ed1694c`; it was superseded rather than accepted. Release 127 promoted source `db3cb44465896b801c63b7227e20ec14df70fe6f` in image `deployment-01M152QG9XBW7B81XQKTXCZPDN`, restored the marker to the actual confirmation input, and exposed a substantive domain gap: a Mission linked to `Consistency & capacity` earned reviewed capability XP but the capability history counted only a Thread's primary capability.

Exact source `7c45bb60e8e0bc567c192740d50e1098426b0739` closes that gap by deriving each Thread focus from every connected skill-node capability and deduplicating each capability/Thread pair. History now retains the owning Thread once even when multiple nodes map to the same durable capability. Required CI `33211557721` passed both lanes, including the complete migration history, 131-entry release convergence, authenticated connected-capability focus journey, recovery artifacts, and isolated logical backup/restore. Fly release 128 promoted image `deployment-01M153K7Z3AJ6723Y0J75V2H9Q` with digest `sha256:08b1d2c9fa6262942c4214c8ee49cf6a324f192c9c0c5bad654c7f18e15e374a`. Fly reported version 128 with its HTTP check passing; fresh `/api/release` returned that exact source and image, `Production`, and all 131 migrations through `0139_affirmation_reset_choice`; `/api/ready` returned ready and anonymous `/api/auth/me` returned `401`. Independent production monitor `33211963856` passed.

Acceptance-harness source `432f0bc87f1c43461a93604569e85a37ba1ce5d1` then passed both required CI lanes in `33214730161`. It corrected only evidence collection: it avoids a `tsx` browser-serialization helper leak, recognizes both explicit and implicit HTML labels, activates the real rendered deletion control without overlay interference, and excludes only deliberate navigation-cancelled read-only GETs while keeping server errors and every other failed request fatal. Protected production-browser run `33214898822` bound that harness to runtime `7c45bb60`. The broad report passed all 84 public and authenticated desktop/mobile route cases with zero failures. Core-loop contract `lyfeos.production-core-loop-acceptance.v7` created one rendered skill-linked Mission, persisted the proof plan, withheld progression for unreviewed evidence, awarded exactly 10 activity XP on completion and exactly 10 reviewed capability XP only after positive self-review, granted no certification or authority, rendered the five-node graph plus the non-primary `Consistency & capacity` focus/history, and restored the exact baseline through rendered reopen and linked reversal. Its bounded automation preview, enable/pause, disabled schedule authoring/revision, and deletion produced no execution receipt, follow-up Mission, or progression change; cleanup deleted the automation and archived the Mission.

Disposable onboarding contract `lyfeos.production-onboarding-acceptance.v2` independently registered one desktop and one mobile local-password account through the rendered production UI. Both rejected a too-short username, advanced only after a unique name was available, presented no verification-code ceremony, persisted in-progress onboarding through reload, completed Mission IDs `0` through `7` exactly once, created an active onboarding-derived Thread, completed onboarding, established a session that survived reload, and rendered one main landmark with no duplicate IDs, unnamed controls, overflow, same-origin server errors, failed requests, or unexpected console errors. Both accounts were then erased through the actual Profile control; each session returned `401`, and both the email and display name became available again. Cleanup was `rendered-erasure` for both. This proves the native local email/password production lifecycle at the automated desktop/mobile browser boundary. It does not prove recovery-email delivery, Clerk/OAuth sign-up, human screen-reader comprehension, real-device behavior, provider routing, or longitudinal outcomes.

### Exact-source rendered Messages lifecycle qualification

Exact CI `33217293801` passed both required lanes for source `5be05ea599e2f27d8597654f7431ebba125cb3be`. The source lane passed TypeScript, 132 local test files with 678 passing and 174 explicitly gated tests, the production client/server/release build, bundle budgets and critical production dependency audit. The PostgreSQL lane applied the complete raw migration history, converged all 131 release migrations, passed the authenticated product suite, uploaded the existing recovery, AI-memory and successor-focus artifacts, completed the logical backup/isolated-restore rehearsal, and added SHA-bound contract `lyfeos.isolated-messages-browser.v1`.

The Messages artifact registered two disposable local-password accounts and exercised the real production-bundle UI. The sender discovered the recipient, created a direct conversation and sent a message; the recipient opened it, produced read evidence, reacted and replied; the sender edited the original and added an author-only private note; the recipient verified note isolation and completed block/unblock. Sender and recipient views at desktop 1440x900 and mobile 390x844 each rendered two messages with one main landmark, no duplicate IDs, unlabeled controls or horizontal overflow. Application console errors, page errors, failed requests and server errors were all zero. Expected fake local Clerk-resource DNS failures were recorded separately rather than hidden. Rendered account erasure left zero users, conversations, participants, messages, notes, reactions and receipts. This is exact-source isolated Chromium evidence, not deployed production-account, real-device, human screen-reader, external-provider delivery or longitudinal-use evidence. Production remains release 128 on source `7c45bb60e8e0bc567c192740d50e1098426b0739` until this behavior-bearing accessibility increment is promoted and independently requalified.

## Schema rollback policy

LyfeOS database migrations are forward-only, numbered, additive where possible, and idempotent in the release runner. Do not run an improvised down migration in production. Before traffic moves, verify that the prior application release remains compatible with the post-migration schema. If it is compatible, an application-image rollback may use the existing schema; record the image identity and do not mark the database as rolled back. If it is not compatible, keep or freeze the current application and ship a reviewed forward-fix migration. Any destructive correction requires an explicit change window, a fresh provider snapshot/restore point, scoped impact verification, and approval. A managed preview/staging migration and rollback rehearsal is still required before a production release that changes schema.

## Incident response

1. Confirm `/api/health` and `/api/ready`.
2. If the health-integrity monitor fired, inspect its aggregate category and the corresponding sanitized import/connection receipts; do not copy health payloads into an incident issue.
3. Inspect Fly logs using the `x-request-id` returned to the affected user.
4. If a release is the cause, roll back to the last healthy Fly image before modifying data.
5. If data integrity is affected, freeze writes, take a snapshot, and restore only through the provider-approved procedure.

## External operating gates

These controls require separate provider configuration or ownership before claiming mature SaaS operations: product analytics consent and retention policy, billing/entitlement controls, support intake, and provider-specific operational ownership. The repository owner is the current recovery-drill owner. `.github/workflows/managed-recovery-drill.yml` opens or refreshes an assigned managed point-in-time recovery ticket on the first day of every month; closing it requires isolated-provider restore evidence and cleanup, not only CI.

### External-provider closure register (2026-08-28)

| Provider | Verified state | Remaining promotion evidence |
| --- | --- | --- |
| Fly | Release 118 exposes immutable source `a77618db7929c33553da5639df803c317a1bf3b7`, image `deployment-01M14TRF5RQY974NGRE0WMEBYS`, `Production`, and 131 migrations through `0139_affirmation_reset_choice` at `/api/release`; readiness, machine health, anonymous-auth rejection, and protected Health integrity passed independent monitor `33200606928`. | Preserve exact source/image/ledger identity and post-deploy monitoring on every behavior-bearing promotion. |
| GitHub Actions | Required `Verify` gates the 847-test/build/audit suite, all authenticated product journeys, 131 release migrations, production dependency audit, disposable rendered automation, AI-memory and successor-focus browser evidence, and isolated logical backup/restore. Exact CI `33200152823` qualified both required lanes for deployed source `a77618db7929c33553da5639df803c317a1bf3b7`; each browser artifact binds its report to an immutable source SHA. Production-browser run `33198882514` separately bound preceding deployed source `325c92f732bee319a166b45ad64a5a51d3b0c7d1` to repaired harness `ff7c16b5d3499f72c94c3d1054ff31b22b545246`, passed 84/84 route-viewports and the v7 truthful Mission core loop, and retained both reports. Manual extension publishing remains bound to committed manifests, write-only signing custody, and the protected production endpoint. | Preserve branch protection, protected publishing custody, exact source/harness qualification, and current evidence links. Keep production append-only automation execution, provider routing, human screen-reader, real-device offline/reconnect, and field performance distinct from disposable Chromium evidence. |
| Sentry | Production DSN, environment and immutable release attribution are deployed. Exact-revision production run `33019828416` sent and flushed one protected content-free probe and validated its provider event identifier. | Confirm the accepted event in Sentry and prove a routed test alert reaches its named recipient. |
| PostHog | Dedicated project `LYFEOS` and restricted privacy-safe lifecycle dashboard exist. The provider reports no event has ever been ingested, and a 2026-08-28 audit reconfirmed client-IP discard is off; autocapture, web-vitals/console/performance capture, session recording and heatmaps are enabled; raw-event retention is 84 months and not explicitly enforced. Fly has none of the five PostHog values. Source now preflights exact project identity and every required provider privacy/retention control before opt-in, while preserving withdrawal during a provider outage or drift. | Disable the unsafe provider defaults, enable IP discard, choose and enforce retention, create a key limited to `project:read`, `person:read`, and `person:write`, install all five secrets together, then complete opt-in/event/withdrawal/deletion proof. |
| Clerk | Production custom domains are verified. The signed Svix endpoint subscribes to create/update/delete, its secret is deployed, provider delivery succeeds, unsigned delivery returns `401`, and a marked synthetic account proved canonical create, update, full provider-owned deletion and cleanup. LyfeOS local authentication remains the primary no-verification path. | Complete real-browser/mobile production sign-up, session and recovery acceptance; the provider lifecycle itself is qualified. |
| Neon | Production is healthy; application-controlled logical restore is continuously proven in isolated CI. The restore window is seven days. Managed branches proved aggregate/schema parity and a 17-test production-mode account/session/mission/Project journey with exact baseline and branch cleanup. The monthly recovery workflow assigns the repository owner an isolated managed-restore checklist and preserves any overdue ticket instead of silently opening duplicates. | Complete each assigned monthly ticket with provider evidence; add production-sized timing and organization response rehearsal at scale. |
| Google | Configuration-aware routes fail closed. The deployed `0131` release adds encrypted server-only custody, mandatory legacy migration, actual-grant capability gates, anti-forgery expiry, provider-sourced Task imports, sanitized errors and truthful revocation status. No production OAuth client credentials are installed. | Create the production OAuth client and approved consent screen, install credentials, and complete issuance/scope/refresh/reconciliation/revocation evidence. |
| Health providers | Provider-neutral consent/sync/provenance foundations and an authenticated encrypted credential vault exist. Apple Health, Health Connect and Oura maps are reviewed. Oura has a current factual-document extractor plus server-only OAuth, encrypted refresh-token rotation, bounded collection-page pulls, correction overlap, scope filtering, sanitized retry receipts and best-effort provider revocation. The production vault key is installed, but Oura client credentials are not. | Supply and approve an Oura client, install its client ID/secret/redirect, then prove real authorization, pagination, correction, refresh and revocation against an authorized account. Select and authorize native bridges and any additional direct providers. |
| Food catalog | Source-attributed gateway and explicit-save contracts exist without a selected dataset. | Select a licensed provider and territory, approve attribution/retention terms, install gateway credentials, and prove known/unknown barcode and deletion behavior. |
| UMH receivers | LyfeOS signed ingress/outbox and leased delivery are deployed but unconfigured. | Install the agreed receiver URL/signing material and prove signed retry/rejection/round-trip evidence with each product receiver. |
| AI providers and extensions | Provider-neutral local/hybrid/cloud resolution, Anthropic and installation-controlled adapters, immutable policy receipts, signed package verification, OST trusted-publisher custody and protected manual publishing are deployed. No model credential/endpoint or reviewed package manifest is installed; both execution and catalog therefore fail closed. | Install one authorized provider or endpoint and qualify an approved run/failure/retry. Approve and commit a first-party manifest before publishing; retain the empty catalog until review is complete. |
| Billing | Entitlement shell exists without an approved commercial model. | Decide plans, prices, tax/refund policy and legal entity before creating live products or accepting payment. |
