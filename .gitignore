node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz.env 

# WP-P4-SECRETS-001: real env files are secret-bearing — never commit.
.env
.env.*
!.env.op.tpl
!.env.tpl
!.env.example

# Local database exports may contain user data, provider tokens, or billing data.
dump.sql
*.dump
*.backup
